import { useState } from "react";
import type { BoardSizeType } from "./board.types";

interface useBoardSize {
  boardSize: BoardSizeType;
}

export function useBoardSize({ boardSize }: useBoardSize) {
//   const []
  return {};
}
